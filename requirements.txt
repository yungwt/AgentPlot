# AgentPlot backend dependencies
# Install:  pip install -r requirements.txt
# After install (for SVG → PNG rendering):
#     playwright install chromium

# --- Backend API ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# --- Data validation ---
pydantic>=2.6

# --- Environment / config ---
python-dotenv>=1.0

# --- LLM (OpenAI-compatible) ---
langchain-openai>=0.1
langchain-core>=0.1

# --- PPT generation ---
python-pptx>=0.6.21

# --- SVG → PNG rendering (headless Chromium) ---
playwright>=1.40

# --- Image / data helpers ---
Pillow>=10.0
pandas>=2.0